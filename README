@ headers

utility.h
  $ operator<< for std::vector
  $ error handling cpp macros

cornucopia.h
  $ container of everything
  > requires --std=c++14

registry.h
  $ factory: constructs object given type (std::string)

configurer.h
  $ configuration parser
  > requires --std=c++14

examples:
  + example.cpp
  + example.conf

  clang++ --std=c++14 example.cpp -o example -O2
  ./example example.conf
